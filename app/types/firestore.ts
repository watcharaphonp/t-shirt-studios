type userProfile = {
    username: string
    fullName: string
    email: string
    isEnableSubscription: boolean
    userRefId: string
    createdAt: string
    updatedAt: string
}

export type { userProfile }
